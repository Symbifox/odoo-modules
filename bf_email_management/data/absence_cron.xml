<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!--
          Deux passes distinctes : détecter les absences à l'agenda est une
          lecture fréquente et sans effet de bord ; refuser des invitations
          écrit dans l'agenda de quelqu'un. Les séparer permet d'éteindre la
          seconde sans perdre la première.
        -->
        <record id="ir_cron_absence_calendar" model="ir.cron">
            <field name="name">Courriels : détecter les absences à l'agenda</field>
            <field name="model_id" ref="model_bf_email_absence"/>
            <field name="state">code</field>
            <field name="code">model._cron_sync_calendar()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">30</field>
            <field name="interval_type">minutes</field>
            <field name="active" eval="True"/>
        </record>

        <record id="ir_cron_absence_decline" model="ir.cron">
            <field name="name">Courriels : refuser les invitations pendant une absence</field>
            <field name="model_id" ref="model_bf_email_absence"/>
            <field name="state">code</field>
            <field name="code">model._cron_decline_meetings()</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="active" eval="True"/>
        </record>
    </data>
</odoo>
