<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="bf_dashboard_action" model="ir.actions.client">
        <field name="name">Tableau de bord</field>
        <field name="tag">bf_dashboard</field>
    </record>

    <!--
        Le menu est ce qui manquait. Jusqu'a la 18.0.1.2.0 ce module ne
        declarait aucun menuitem : l'ecran n'etait atteignable que si son
        post_init_hook avait fait de son action l'accueil de l'usager. Sur les
        deux locataires qui portent les deux modules, l'accueil est celui de
        bf_home — donc quatre modules calculaient et rendaient des cartes dans
        un ecran qu'on n'ouvrait que par /odoo/action-<id> tape a la main.

        Sequence 2 : juste apres l'accueil de bf_home, qui reste la porte.
        Pas de web_icon tant qu'une icone n'a pas ete versee au dossier
        d'icones approuvees ; Odoo rend une tuile par defaut d'ici la.
    -->
    <menuitem id="menu_bf_dashboard_root"
              name="Tableau de bord"
              action="bf_dashboard_action"
              sequence="2"/>

</odoo>
